import { View, Text } from 'react-native';

export default function Session() {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Prayer Session Screen</Text>
        </View>
    );
}
